#!/usr/bin/node

const myObject = {
    type: 'object',
    value: 12
  };
  
  console.log(myObject);
  
  // Update the value property of myObject
  myObject.value = 89;
  
  console.log(myObject);
  