-- Count the number of rows in 'first_table' where the id column equals 89
-- This query returns a single number representing how many records match the condition
-- Useful for checking if a specific ID exists or counting duplicate IDs
SELECT COUNT(*) FROM first_table WHERE id = 89;
