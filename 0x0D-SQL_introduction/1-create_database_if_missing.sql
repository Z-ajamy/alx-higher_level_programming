-- Create a new database named 'hbtn_0c_0' if it doesn't already exist
-- The IF NOT EXISTS clause prevents errors if the database is already present
-- Backticks around the database name handle any special characters or reserved words
CREATE DATABASE IF NOT EXISTS `hbtn_0c_0`;
