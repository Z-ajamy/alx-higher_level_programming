-- Display the complete CREATE TABLE statement used to create 'first_table'
-- This shows the exact DDL (Data Definition Language) that would recreate the table
-- Includes all constraints, indexes, engine type, charset, and other table options
-- Backticks protect the table name from conflicts with SQL reserved words
SHOW CREATE TABLE `first_table`;
