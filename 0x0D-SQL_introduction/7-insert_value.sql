-- Insert a new row into the 'first_table' table
-- Values are provided in the same order as table columns: id, name
-- Using VALUES clause to specify the data for the new record
INSERT INTO first_table VALUES (89, "Best School");
