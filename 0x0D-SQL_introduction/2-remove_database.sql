-- Delete the database named 'hbtn_0c_0' if it exists
-- The IF EXISTS clause prevents errors if the database is not present
-- WARNING: This will permanently delete the database and ALL its data
-- Backticks around the database name handle any special characters or reserved words
DROP DATABASE IF EXISTS `hbtn_0c_0`;
